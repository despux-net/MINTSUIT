media: assets

content:
  - name: notice
    label: Holding page
    path: data/notice.txt
    type: file
    format: raw
    fields:
      - name: body
        label: Notice
        type: text
        description: >-
          The text shown beside MINT SUIT on the holding page. Write it as it
          should read; a blank line starts a new paragraph. Leave it empty and
          the page keeps its own default.
